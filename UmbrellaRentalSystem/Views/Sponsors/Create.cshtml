@model UmbrellaRentalSystem.Models.Sponsor

@{
    ViewData["Title"] = "新增贊助商";
}

<div class="container mt-4 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <!-- 仿新增雨傘資料風格卡片 -->
            <div class="card shadow-sm border-0 rounded-3">
                <!-- 深色標題欄 -->
                <div class="card-header bg-dark text-white text-center py-3 rounded-top">
                    <h4 class="mb-0 fw-bold">新增贊助商資料</h4>
                </div>

                <div class="card-body p-4">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <!-- 1. 贊助商名稱 -->
                        <div class="mb-4">
                            <label asp-for="SponsorName" class="form-label fw-bold text-dark">1. 贊助商名稱</label>
                            <input asp-for="SponsorName" class="form-control form-control-lg fs-6" placeholder="例如 慈濟基金會 / 遠東新世紀" />
                            <span asp-validation-for="SponsorName" class="text-danger small"></span>
                        </div>

                        <!-- 按鈕區：上下疊放滿寬按鈕 -->
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg fs-6 fw-bold py-2">
                                確認新增贊助商
                            </button>
                            <a asp-action="Index" class="btn btn-outline-secondary btn-lg fs-6 py-2">
                                返回贊助商清單
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}